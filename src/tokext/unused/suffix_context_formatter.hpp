
#ifndef SUFFIX_CONTEXT_FORMATTER_HPP_QPOMIBRG
#define SUFFIX_CONTEXT_FORMATTER_HPP_QPOMIBRG

namespace tokext
{
/*
    typedef std::string (GeneralizedSuffixArray::*char2_fp_t) (xchar_t) const;
    std::string char2alnum(xchar_t encChar) const;
    std::string char2code(xchar_t encChar) const;

    typedef std::string (GeneralizedSuffixArray::*substr_info_fp_t) (idx_t idx, idx_t len) const;
    std::string substr_formatter(idx_t idx, idx_t len, char2_fp_t trans) const;

    std::string table_formatter(substr_info_fp_t trans) const;

    // to string

    std::string code_substr(idx_t idx, idx_t len) const;
    std::string code_table() const;

    std::string alnum_substr(idx_t idx, idx_t len) const;
    std::string alnum_table() const;
    */
} /* tokext */

#endif /* end of include guard: SUFFIX_CONTEXT_FORMATTER_HPP_QPOMIBRG */
